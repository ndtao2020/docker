version: "3.9"

volumes:
  redis-db:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${DIR_PATH_HOST}/redis/data
  postgres-db:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${DIR_PATH_HOST}/postgres/data

secrets:
  # echo ${POSTGRES_USER} | docker secret create postgres-root-user -
  postgres-root-user:
    file: ./postgres/secrets/root_user.txt
  # echo ${POSTGRES_PASSWORD} | docker secret create postgres-root-password -
  postgres-root-password:
    file: ./postgres/secrets/root_password.txt

services:
  redis:
    container_name: redis
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    ports:
      - "${REDIS_PORT}:6379"
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    volumes:
      - redis-db:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    healthcheck:
      test: [ "CMD", "redis-cli", "--raw", "incr", "ping" ]
      interval: 20s
      timeout: 5s
      retries: 5

  postgres:
    container_name: postgres
    image: postgres:16-alpine
    restart: unless-stopped
    secrets:
      - postgres-root-user
      - postgres-root-password
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres-db:/var/lib/postgresql/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_USER_FILE: /run/secrets/postgres-root-user
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres-root-password
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 20s
      timeout: 5s
      retries: 5

  helloworld:
    container_name: helloworld
    image: crccheck/hello-world
    ports:
      - 8000:8000

  # step 2: docker compose up -d helloworld nginx
  nginx:
    container_name: nginx
    restart: unless-stopped
    image: nginx:stable
    ports:
      - 80:80
      - 443:443
    environment:
      HOST_DOMAIN: ${HOST_DOMAIN}
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot

  # step 3: docker compose up -d certbot
  # step 4: docker logs certbot
  # step 6: crontab -e
  # step 7: 0 5 1 */2 * /usr/bin/docker compose -f ${DIR_PATH_HOST}/docker-compose.yml up certbot
  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes: 
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    command: certonly --webroot -w /var/www/certbot --force-renewal --email ${HOST_CERTBOT_EMAIL} -d ${HOST_DOMAIN} --agree-tos
